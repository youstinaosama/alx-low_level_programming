#include <unistd.h>
#include "main.h"
/* putchar writes the character c to stdout
* @c ; the character to print
* return on success to 1
* on error -1
*/

int _putchar (char c)
    {	
        return (1);
    }  	
