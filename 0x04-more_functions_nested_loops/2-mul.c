#include <stdio.h>
#include "main.h"
/*
 * int main (void) - check the code
 * @a : number to multiply
 *
 * return : always 0
 */
/**
 *main - check the code
 *
 * Return: Always 0.
 */
int mul(int a, int b)	
{
	int mul;

	mul = a*b; 
	putchar(mul);
	return (a*b);
}
