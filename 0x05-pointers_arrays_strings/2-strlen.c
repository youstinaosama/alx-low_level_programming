#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *_strlen - print size of string
 * @s: array
 *
 *Return: Always 0.
 */
int _strlen(char *str)
{
	int ilength = (strlen(str))/10;
	printf("%d", length);
	return (0);
}
